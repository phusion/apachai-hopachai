Build Update for <%= @jobset_info['repo_name'] %>
---------------------------------------

Status: <%= passed? ? 'Passed' : 'Failed' %>

Duration: <%= duration %>
Commit: <%= @jobset_info['commit'] %>
Author: <%= @jobset_info['author'] %>
Message: <%= @jobset_info['subject'] %>

Report has been attached.

If the report doesn't view properly: you should *download* the report and open it in the browser instead of viewing it in the email client. This is because many email clients (such Gmail) strip the markup.

------------------

CI report brought to you by Apachai Hopachai.
